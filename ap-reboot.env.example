# Copy to /etc/iserv/server-unifios/ap-reboot.env and chmod 0600.
# Set enabled=true only when daily access-point reboots are desired.
enabled=false
api_key=
# base_url defaults to https://$(hostname -f).
# base_url=https://unifi.example.invalid
site=default
name_prefix=UAP-
